{
    "model_name": "model_10",
    "steps": 2000000,
    "epsilon_start": 0.035,
    "min_epsilon": 0.005,
    "epsilon_decay_rate": 1.5000000000000002e-08,
    "target_network_update_interval": 10000,
    "learning_rate": 0.0005,
    "gamma": 0.99,
    "memory_length": 350000,
    "batch_size": 128,
    "input_size": 39,
    "timeout": "min(environment.size ** 2 + environment.size, environment.size ** 2 * 0.2 + len(environment.snake) * 1.25)",
    "starting_lengths_used": "33% 2-75, 33% 76-150, 33% 151-300",
    "other_notes": "Starting with model_9_step_1600000, I trained for 2,000,000 steps for one last experiment. I choose this model because it was right before a dip in performance began at the end of training."
}